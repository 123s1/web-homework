# 博物馆预约系统 · 后续完成步骤文档

> 本文档在「核心代码 / 数据一致性 / 并发控制 ≈ 已基本满分」之后，列出**剩余交付物类工作**。
> 每项标注：做什么、谁负责、需要你提供什么、完成标准。
> 优先级顺序：**合并测试分支 → 部署(已完成) → 测试报告 + PDF → 演示视频 → 最终打包**。
>
> 最后更新依据：已读取 `main` 及全部分支（`devin/1780162179-feature-gaps`、`devin/1780165338-linux-deploy`、`devin/1780190451-tests-and-reports`）的实际代码与文档后整理。

---

## 0. 分支与 PR 现状（重要）

| 分支 / PR | 内容 | 状态 |
|---|---|---|
| `main` | 主线 | 已合并 PR #1、PR #2 |
| PR #1 `feature-gaps` | 单人预约上限 / 请求量统计 / 真实二维码 | ✅ 已合并 |
| PR #2 `linux-deploy` | Linux/VMware 一键部署脚本与说明 | ✅ 已合并 |
| `devin/1780190451-tests-and-reports` | **功能测试报告 + 性能测试报告 + 10 张截图 + 压测脚本(含 JMeter) + 一个并发修复** | ⚠️ 已推送但**未开 PR、未合并** |

> 关键修正：原计划里把「B 功能测试报告」「C 性能测试报告」标为待做，实际上这两份报告与配套截图/脚本**已在上面这条分支里做好了**，只是没合并到 `main`。所以第一步应当是把它合并进主线（见下方步骤 0.1）。

### 0.1 合并测试分支（最优先）

**做什么**
- 给 `devin/1780190451-tests-and-reports` 开 PR 合并到 `main`。该分支额外带来：
  - `project-docs/功能测试报告.md`（FT-A1~FT-C4 浏览器端实测 + 结论）
  - `project-docs/性能测试报告.md`（不超额 / 不重复 / 稳定性 + 压测结果表）
  - `project-docs/并发与一致性.md`
  - `project-docs/screenshots/` 共 10 张 UI 截图（FT-01 首页、FT-A3 预约二维码、FT-B2 并发不超额、CSV 导出、系统状态等）
  - `scripts/perf/`：`concurrency_test.sh`（N 线程抢 M 名额）、`museum-reservation.jmx`（JMeter）、`generate_idcards.sh`
  - **一个 `main` 里还没有的并发修复**：并发创建游客时捕获 `DuplicateKeyException` 回查 `findByIdCard`，避免唯一键冲突报错（`ReservationService.getOrCreateVisitor`）。
- 注意：该分支是从初始提交切出的，与 `main` 合并时需保留 `main` 上的 `deploy/` 目录（部署脚本不能被删）。建议用「合并 main → 该分支」或挑拣(cherry-pick)方式，避免误删 PR #2 的部署文件。

**谁负责**：Devin（需你同意开 PR）。
**完成标准**：测试报告、截图、压测脚本、并发修复进入 `main`，且 `deploy/` 目录仍完整保留。

---

## A. Linux/VMware 部署脚本（已完成）

- 产出：`museum-reservation-system/deploy/`（一键脚本 `deploy-all.sh` + `00~03` 分步脚本 + systemd/Nginx 模板 + README）。
- PR：https://github.com/123s1/web-homework/pull/2 （已合并）
- 架构：宿主机浏览器 → Nginx:80（托管前端 dist + 反代 `/api`）→ Spring Boot(systemd, :8080) → MySQL 8。
- 你这边只需在真机虚拟机跑一遍（见 E）。

**状态**：✅ 已完成。

---

## B. 功能测试报告 + 截图

**现状**：已在分支 `devin/1780190451-tests-and-reports` 完成
- `project-docs/功能测试报告.md`：覆盖游客主流程（首页 → 登录 → 时段 → 预约 → 二维码 → 我的预约）、异常（身份证格式校验、名额已满、重复预约）、管理员（登录 → 统计 → 预约名单 → CSV 导出 → 系统状态）。
- `project-docs/screenshots/`：10 张对应截图。
- 另有 `project-docs/功能测试自动执行记录.md`（接口层 FT-001~027，已在 `main`）。

**还需做**
- 合并到 `main`（见 0.1）。
- 可选补充：若界面后续有改动，重跑相关用例并更新截图。

**谁负责**：Devin。
**完成标准**：报告含用例/步骤/预期/实际/截图，结论齐全，且已在 `main`。

---

## C. 性能 / 并发测试报告

**现状**：已在分支 `devin/1780190451-tests-and-reports` 完成
- `project-docs/性能测试报告.md`：含测试环境、并发方案说明、不超额测试表（如 50 名额 / 500~1000 请求 / 100~200 并发 → 成功数严格 = 50）、响应时间、稳定性结论。
- `scripts/perf/concurrency_test.sh`：本机「N 线程抢 M 名额」实证脚本（数据一致性 30 分硬证据）。
- `scripts/perf/museum-reservation.jmx`：JMeter 压测计划。

**还需做**
- 合并到 `main`（见 0.1）。
- 可选补充（用你自己的真机数据增强说服力）：把你跑的 JMeter 聚合报告并入报告。

**需要你提供（可选，任选其一）**
- JMeter 聚合报告（Aggregate / Summary Report）截图或导出 CSV；或 `.jtl` 结果文件；
- 或关键数字：并发线程数、循环次数、总请求数、TPS/吞吐、平均/90%/95%/99% 响应、错误率，及被测接口（应含 `POST /api/reservations`）。

**谁负责**：Devin 整理（你提供数据为可选增强）。
**完成标准**：报告含测试方案、工具、结果表、响应时间统计、稳定性结论 + 不超额实证，且已在 `main`。

---

## D. 系统设计文档终稿 + 全部转中文 PDF

**做什么**
1. 把 `project-docs/系统设计文档初稿.md` 补成终稿，确保覆盖：需求描述、功能模块、系统架构、数据库设计、核心业务流程、并发处理方案、**三种方案对比及选型理由**（作业明确要求，务必补全）。
2. 在 Devin 机器装好带中文字体的转换工具（pandoc + 中文字体，或 weasyprint），把以下文档统一导出**中文 PDF**：
   - 系统设计文档（终稿）、数据库设计、需求拆分、后端接口设计、前端开发设计；
   - 功能测试报告（B）、性能测试报告（C）。

**需要你提供**：无。
**谁负责**：Devin。
**完成标准**：PDF 中文显示正常、排版规范、可直接提交。

---

## E. 虚拟机真机部署演示（VMware 8GB）

**做什么（你在自己电脑的 VMware 上，用 PR #2 的脚本）**
1. VMware 新建虚拟机，装 Ubuntu 22.04 / CentOS，**内存固定 8192MB**，联网。
2. 拷入项目 → `cd museum-reservation-system/deploy && cp deploy.env.example deploy.env`（按需改密码）→ `sudo ./deploy-all.sh`。
3. 放行 80 端口，宿主机浏览器访问 `http://<虚拟机IP>/` 验证全部功能。
4. 截图留证：`free -h`（8G 内存）、`java -version`、`systemctl status museum-backend`、`/api/health`、宿主机访问页面。

**需要你提供**：这一步需在你的 VMware 上跑（Devin 无法操作你的 VMware）；脚本已就绪，报错把日志发我。
**谁负责**：你（Devin 协助排错）。
**完成标准**：宿主机能访问、内存为 8G、`museum-backend` 服务 active。

---

## F. 系统演示视频（≤5 分钟）

**做什么**：录一段完整演示，建议分镜（覆盖评分点）：
1. 部署环境：VMware 虚拟机 8G、`/api/health` UP（约 30s）。
2. 后台配置：管理员登录 → 配置活动 / 时段 / 名额（约 60s）。
3. 前端预约：游客登录 → 选时段 → 预约成功 → 二维码；再演示重复预约 / 名额已满 / 时间拦截（约 90s）。
4. 并发测试：JMeter 跑一次，展示无超额、名额精确（约 60s）。
5. 数据核对：后台预约记录 / 剩余名额与配置一致（约 30s）。

**需要你提供**：最终录制需你来录（声音 + 真机）；Devin 可先把**逐句解说脚本 + 分镜表**写好给你照着念。
**谁负责**：你（Devin 出脚本）。
**完成标准**：清晰、有声、≤5 分钟、覆盖上述 5 段。

---

## G. 最终打包

**做什么**：按要求打成 `学号-姓名-博物馆预约系统.zip`，内含：
- 源代码工程（可直接编译运行，含 `deploy/` 脚本与数据库脚本）；
- 系统设计文档（PDF）、功能测试报告（PDF）、性能测试报告（PDF）；
- 演示视频（或视频链接说明）。

**需要你提供**：你的**学号 + 姓名**（用于压缩包命名）。
**谁负责**：Devin。
**完成标准**：解压后依说明可在 Linux 虚拟机部署运行，文档齐全且为 PDF。

---

## 建议执行顺序（最省时间）

1. **现在**：Devin 给测试分支开 PR 合并到 `main`（步骤 0.1，把 B/C 的报告 + 截图 + 压测脚本 + 并发修复并入主线）。
2. **并行**：Devin 做 D 的工具搭建（pandoc + 中文字体），补系统设计文档终稿（三种方案对比）。
3. **你有空时**：在 VMware 跑一遍部署并截图（E）；如愿意，把 JMeter 数据发我增强 C。
4. 然后：Devin 把全部文档导出中文 PDF（D）；出 F 的解说脚本 → 你录视频。
5. 最后：你给学号 + 姓名 → Devin 打包 G。

---

## 剩余缺口对照评分表

| 编号 | 缺口项 | 对应分值 | 当前状态 | 负责 |
|---|---|---|---|---|
| 0.1 | 合并测试分支到 main | —（前置） | ⬜ 待做（PR 未开） | Devin |
| A | Linux/VMware 部署脚本与说明 | 虚拟机部署 10 分 | ✅ 已完成（PR #2） | Devin |
| B | 功能测试报告 + 截图 | 文档规范 5 分 | 🟡 已完成于未合并分支，待合并 | Devin |
| C | 性能 / 并发测试报告 | 高并发 25 分（证据） | 🟡 已完成于未合并分支，待合并（可补真机数据） | Devin（你可选提供数据） |
| D | 设计文档终稿 + 全部转中文 PDF | 文档规范 5 分 | ⬜ 待做 | Devin |
| E | 虚拟机真机部署演示（8GB） | 虚拟机部署 10 分（实测证据） | ⬜ 待做（脚本就绪） | 你 |
| F | 演示视频（≤5min） | 提交物 | ⬜ 待做（Devin 可先出脚本） | 你 |
| G | 最终压缩包打包 | 提交物 | ⬜ 待做（最后一步） | Devin |
