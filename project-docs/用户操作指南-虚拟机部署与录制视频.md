# 用户操作指南 · 虚拟机真机部署(E) + 演示视频录制(F)

> 本指南是「需要你本人完成」的两件事的**逐步操作手册**:
> - **E. 在 VMware 8GB 虚拟机里真机部署系统**(对应评分:虚拟机部署 10 分的实测证据)
> - **F. 录制系统演示视频**(≤5 分钟,提交物)
>
> 其余交付物(测试报告、文档 PDF、打包脚本、视频解说词)Devin 已完成,见 `dist/pdf/` 与 `museum-reservation-system/scripts/build-deliverables.sh`。

---

# E. 虚拟机真机部署(VMware,固定 8GB)

部署后架构:宿主机浏览器 → Nginx:80(前端 + 反代 `/api`)→ Spring Boot:8080 → MySQL 8。
一键脚本位于 `museum-reservation-system/deploy/`,以下按顺序操作即可。

## E-自检:先判断你属于哪种情况(很重要,先看这段)

不同人起点不同,先对号入座,别盲目重装。

**判断 0:电脑装了 VMware 吗?**
- Windows 左下角搜索框输入 `VMware`:能搜到「VMware Workstation Pro / Player」= 已装,打开它;
- 搜不到 = 没装 → 先去官网下载安装 **VMware Workstation**(个人用 Player 免费),装好再打开。

**判断 1:打开 VMware 后,左侧「库 / Library」里有没有已存在的虚拟机?**
- 列表是空的 → 你需要新建,往下走 E-0、E-1。
- 列表里已有虚拟机 → 看判断 2,可能直接复用,不用重建。

**判断 2:已有的虚拟机能不能直接复用?**
选中它 →「开启此虚拟机」:
- 能正常进入 Linux 桌面/登录界面 → 登录后打开终端执行自检:
  ```bash
  cat /etc/os-release       # NAME 是否为 Ubuntu(20.04+;CentOS 7/8 也可)
  free -h                   # 内存 total 是否约 7.7Gi(=8G)
  nproc                     # CPU 核数,≥2 较好
  ping -c 2 www.baidu.com   # 能 ping 通 = 可联网
  ```
  - 是 Linux 且能联网 → **不用重装!直接跳到 E-2 / E-3 拿代码部署。**
  - 内存不是 8G → 在虚拟机里关机 → VMware 右键该虚拟机「设置 → 内存」改成 **8192** → 再开机。
  - 不能联网 → 检查该虚拟机「设置 → 网络适配器」是否为 NAT 或桥接,再试。
- 进去发现是 Windows 或系统太旧装不动软件 → 直接新建一台,走 E-0、E-1。

**判断 3:有虚拟机条目但开不进系统(黑屏/卡 BIOS/从没装过 OS)**
= 空壳虚拟机,需要挂 ISO 装系统:按 E-0 下好 ISO →「设置 → CD/DVD → 使用 ISO 映像文件」选中 → 开机后按 E-1 第 7 步安装。

**一句话总结**
- 没 VMware → 先装 VMware;
- 有 VMware、没虚拟机 → 新建(E-0 / E-1);
- 已有 Linux 虚拟机且能联网 → 直接复用,跳到 E-2 部署(内存不是 8G 就先改 8192)。

---

## E-0. 前置:下载 Ubuntu 22.04 镜像(ISO)

浏览器打开 <https://releases.ubuntu.com/22.04/>,下载 `ubuntu-22.04.x-desktop-amd64.iso`(约 4–5GB,桌面版,便于录视频),记住保存路径。

## E-1. 新建虚拟机(VMware Workstation 17 向导逐屏操作)

> 以下对应「新建虚拟机向导」的每一屏,按顺序点即可。

1. **欢迎页**:选「**典型(推荐)(T)**」→ 下一步。
2. **安装客户机操作系统**:选「**稍后安装操作系统(S)**」→ 下一步。
   (选这个最稳,避免 VMware「简易安装」自动建账户出问题)
3. **选择客户机操作系统**:客户机操作系统选「**Linux(L)**」;版本下拉选「**Ubuntu 64 位**」→ 下一步。
4. **命名虚拟机**:名称如 `museum-ubuntu`;位置选空间够的盘(≥40GB 可用)→ 下一步。
5. **指定磁盘容量**:最大磁盘大小填 **40**(GB,至少 20);选「**将虚拟磁盘拆分成多个文件(M)**」→ 下一步。
6. **已准备好创建虚拟机**:点「**自定义硬件(C)…**」(关键,在这里设内存并挂载 ISO):
   - **内存**:设为 **8192 MB(=8GB)** —— 作业硬性要求,必须是这个值。
   - **处理器**:总核数 ≥ **2**。
   - **新 CD/DVD (SATA)**:右侧选「**使用 ISO 映像文件(M)**」→ 浏览 → 选中 E-0 下载的 Ubuntu ISO。
   - **网络适配器**:保持「**NAT**」(默认,保证宿主机能访问虚拟机 IP;也可用桥接)。
   - 点「**关闭**」→ 回到向导点「**完成**」。
7. **安装系统**:选中虚拟机 → 「**开启此虚拟机**」→ 进入 Ubuntu 安装界面:
   - 选语言 → 「Install Ubuntu」→ 键盘默认 → Normal/Minimal 均可。
   - 安装类型选「**清除整个磁盘并安装 Ubuntu**」(只清虚拟磁盘,不动真机)。
   - 选时区 → **设置用户名和密码(记牢,后面 sudo 要用)** → 等待安装完成 → 「**Restart Now**」重启。
   - 重启若提示移除安装介质,直接回车。
8. 进入系统后确保能联网(脚本要在线装 JDK/Node/MySQL/Nginx),即可进入 E-2。

> 验证内存:终端执行 `free -h`,Mem 行 total 约 7.7Gi 即为 8G(8G 通常显示约 7.7G,正常)。

## E-2. 查看虚拟机 IP(后面宿主机访问要用)

```bash
ip addr | grep inet
```
记下类似 `192.168.x.x` 的地址(NAT 通常是 192.168.x.x,桥接是你局域网网段)。

## E-3. 把项目拷进虚拟机

任选一种:
- **方式 A(推荐,联网克隆)**:
  ```bash
  sudo apt-get update && sudo apt-get install -y git
  git clone https://github.com/123s1/web-homework.git
  cd web-homework/museum-reservation-system/deploy
  ```
- **方式 B(共享/U盘/拖拽)**:把整个 `museum-reservation-system` 目录拷到虚拟机,例如 `~/museum-reservation-system`,然后:
  ```bash
  cd ~/museum-reservation-system/deploy
  ```

> 注意:确保拷进去的是**含 `deploy/` 目录**的最新版本(已合并测试分支后的 main)。

## E-4. 准备部署配置

```bash
cp deploy.env.example deploy.env
nano deploy.env        # 按需修改:数据库密码、端口等(默认即可跑通)
```
重点字段(打开看一眼即可,默认值能直接用):
- `DB_PASSWORD`:MySQL 中本系统库的密码(可保持默认或自定义)。
- 端口默认:Nginx 80、后端 8080、MySQL 3306。

## E-5. 一键部署

```bash
sudo ./deploy-all.sh
```
该脚本会依次:安装 JDK21/Node/MySQL/Nginx → 建库导入 `schema.sql` → 构建前端 dist 与后端 jar → 配置 systemd 服务 `museum-backend` 与 Nginx → 启动。

> 若想分步执行,也可依次运行:`00-install-prereqs.sh` → `01-init-database.sh` → `02-build.sh` → `03-deploy.sh`(见 `deploy/README.md`)。

## E-6. 放行 80 端口(如开了防火墙)

- Ubuntu(ufw):
  ```bash
  sudo ufw allow 80/tcp && sudo ufw allow 8080/tcp
  ```
- CentOS(firewalld):
  ```bash
  sudo firewall-cmd --permanent --add-port=80/tcp --add-port=8080/tcp && sudo firewall-cmd --reload
  ```

## E-7. 验证(并截图留证 —— 这是部署 10 分的证据)

在**虚拟机内**:
```bash
free -h                              # 截图:确认内存为 8G(Mem 行 total ≈ 7.7Gi~8.0Gi)
java -version                        # 截图:JDK 版本
systemctl status museum-backend      # 截图:服务 active (running)
curl http://127.0.0.1/api/health     # 截图:返回健康状态(UP/success)
```
在**宿主机浏览器**:
- 打开 `http://<虚拟机IP>/`(用 E-2 记下的 IP)→ 截图首页正常显示。
- 走一遍游客预约 + 管理员登录,确认功能可用 → 截图。

> 把以上截图保存好,既用于答辩,也可放进最终压缩包的「测试截图」或部署说明里。

## E-8. 常见问题排查

| 现象 | 排查 |
|---|---|
| 宿主机打不开页面 | 确认虚拟机 IP、80 端口已放行;`systemctl status nginx` 是否 active |
| 接口 502 | `systemctl status museum-backend`、`journalctl -u museum-backend -n 100` 看后端日志 |
| 建库失败 | 确认 MySQL 已启动 `systemctl status mysql`;`deploy.env` 密码与实际一致 |
| 内存不是 8G | 关机 → VMware 虚拟机设置 → 内存改为 8192MB |

> 部署中如有报错,把对应命令的输出/日志发给我(Devin),我帮你定位。

---

# F. 录制系统演示视频(≤5 分钟)

## F-1. 录制前准备

- 系统已按 E 部署好并可正常访问(宿主机能打开页面)。
- 准备录屏软件(OBS Studio / Windows 自带 Xbox Game Bar `Win+G` / 录屏工具均可),**开启麦克风**。
- 打开需要的窗口:VMware 虚拟机、宿主机浏览器、JMeter(并发演示用)。
- 解说词见 `project-docs/演示视频解说脚本.md`,照着念即可。

## F-2. 录制分镜(总时长控制在 5 分钟内)

| 段落 | 时长 | 画面内容 |
|---|---|---|
| 1 部署环境 | ~30s | 展示 VMware 虚拟机内存 8G(`free -h`)、`curl /api/health` 返回 UP |
| 2 后台配置 | ~60s | 管理员登录 → 配置/查看预约活动、分时时段、名额 |
| 3 前端预约 | ~90s | 游客实名登录 → 选时段 → 预约成功 + 二维码;再演示重复预约/名额已满/时间拦截的提示 |
| 4 并发测试 | ~60s | 运行 JMeter(或 `scripts/perf/concurrency_test.sh`),展示高并发下成功数=名额数、无超额 |
| 5 数据核对 | ~30s | 后台预约记录/剩余名额与配置一致;`booked_count` 未超过 `total_capacity` |

## F-3. 录制要求

- 画面清晰、声音正常、**时长不超过 5 分钟**。
- 覆盖上述 5 段(尤其第 3、4 段是功能与一致性的核心得分点)。
- 录好后命名如 `演示视频.mp4`。

## F-4. 放入最终压缩包

把录好的视频放到打包目录(Devin 打包时会预留 `演示视频请放这里` 目录),或单独提供网盘链接说明。

---

# 完成 E、F 后

把以下东西给 Devin,即可生成最终压缩包(G):
1. 你的 **学号 + 姓名**(用于压缩包命名)。
2. 录好的**演示视频**(或视频链接)。
3. (可选)E-7 的部署截图,用于补充部署证据。
