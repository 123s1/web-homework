# 省博物馆参观预约系统项目要求分析

## 1. 文档来源

本分析基于以下两个要求文档整理：

- **大作业共性要求.docx**
- **web大作业-02-省博物馆参观预约系统.docx**

这两个文档共同说明了本次 Web 大作业需要完成的系统功能、部署环境、并发要求、数据一致性要求、评分标准以及最终提交材料。

---

## 2. 项目总体目标

需要完成一个完整可运行、可部署、可演示的 **某省博物馆参观预约系统**。

系统应围绕“博物馆限量分时预约”业务展开，实现管理员后台和游客前台两个部分，支持游客实名登录、查看场馆信息、选择日期和时段进行预约，并保证在高并发场景下不会出现超额预约、重复预约或名额数据不一致的问题。

最终项目必须能够部署到 **VMware 虚拟机中的 Linux 环境**，并且宿主机可以正常访问系统功能。

---

## 3. 必须完成的核心内容

### 3.1 管理员后台功能

管理员后台是本系统必须实现的管理端，主要负责配置预约规则、维护场馆信息、查看预约情况和监控系统状态。

#### 3.1.1 场馆信息管理

需要实现：

- **开放信息维护**：新增、编辑、维护博物馆开放信息。
- **参观须知维护**：维护游客参观规则、入馆要求等说明。
- **通知信息维护**：设置特殊展览、临时闭馆、公告通知等内容。

#### 3.1.2 预约活动配置

需要实现：

- **每日总名额配置**：设置每天允许参观的总人数。
- **分时时段配置**：设置不同入场时段，例如上午、下午或具体时间段。
- **时段名额配置**：每个时段都有独立预约名额。
- **单人预约限制**：设置单人预约上限、单日可预约次数。
- **预约时间控制**：设置预约开放时间和预约结束时间。
- **预约通道控制**：支持启用或禁用预约通道。
- **名额调整**：管理员可以根据需要调整预约名额数量。

#### 3.1.3 预约与名额管理

需要实现：

- **实时余量查看**：查看每日和各分时时段的剩余名额。
- **预约记录查看**：查看所有成功预约记录。
- **游客信息查看**：预约记录中应包含游客身份信息、手机号、预约时间、参观日期、参观时段等。
- **预约名单导出**：支持导出预约名单。
- **统计数据查看**：统计参观预约数据，例如总预约人数、各时段预约人数等。

#### 3.1.4 系统状态查看

需要实现：

- **在线预约请求量查看**：展示当前系统预约请求情况。
- **预约活动状态查看**：显示预约活动状态，包括未开始、进行中、已结束。

---

### 3.2 用户前端预约功能

用户前端是游客使用的预约入口，需要完成从登录到预约再到查看预约凭证的完整流程。

#### 3.2.1 用户身份验证

需要实现：

- **实名登录**：使用身份证号和手机号进行登录。
- **简单验证即可**：不要求复杂认证，但需要进行基本格式校验。

#### 3.2.2 信息浏览

用户登录后应能查看：

- **场馆开放信息**
- **参观须知**
- **通知公告**
- **预约开放时间**
- **可预约日期和时段**
- **剩余预约名额**
- **参观规则**

#### 3.2.3 核心预约流程

用户需要能够：

- **选择参观日期**
- **选择入场时段**
- **提交预约请求**
- **实时获得预约结果**

预约结果至少应覆盖以下情况：

- **预约成功**
- **名额已满**
- **已超过预约上限**
- **未到预约时间**
- **预约已结束**
- **重复预约失败**

#### 3.2.4 个人中心

需要实现：

- **查看我的预约记录**
- **查看预约成功凭证**
- **查看入园二维码**

二维码可以是简单生成的预约凭证二维码，也可以用预约编号生成，只要能作为演示凭证即可。

---

## 4. 非功能性硬性要求

### 4.1 并发安全

系统需要支持大量用户同时预约，尤其是节假日高峰场景。

要求：

- **支持高并发访问**
- **接口响应正常**
- **服务不崩溃**
- **页面不卡死**
- **预约核心接口需要重点保证稳定性**

### 4.2 数据一致性

这是评分中最关键的部分之一。

必须保证：

- **不能超额预约**
- **不能重复预约**
- **名额数据必须精准一致**
- **多人同时抢同一时段名额时，最终成功人数不能超过配置名额**

其中，文档明确说明：

- **出现超额预约，数据一致性中的超额预约部分直接 0 分**。

### 4.3 时间严格控制

必须保证：

- **未到预约开放时间不能预约**
- **超过预约结束时间不能预约**
- **预约活动结束后不能继续预约**
- **只有在有效预约时间范围内才能提交预约**

### 4.4 可部署性

项目必须可部署到指定环境：

- **VMware 虚拟机**
- **Linux 系统，CentOS 或 Ubuntu 均可**
- **固定分配 8GB 内存，即 8192MB**
- **虚拟机中安装 JDK 和 MySQL**
- **项目打包部署到虚拟机中运行**
- **宿主机能够正常访问系统所有功能**

---

## 5. 技术栈要求

### 5.1 后端

要求使用：

- **Spring Boot**

也允许：

- **Java EE**

建议优先选择 Spring Boot，因为更适合快速完成后台接口、数据库操作和部署。

### 5.2 数据库

要求使用：

- **MySQL**

数据库中至少需要设计以下数据表：

- **管理员表**
- **游客用户表**
- **场馆信息表**
- **通知公告表**
- **预约活动表**
- **预约时段表**
- **预约记录表**
- **系统统计或请求记录表，可选**

### 5.3 前端

可自由选择：

- **HTML**
- **CSS**
- **JavaScript**
- **Vue**
- **Bootstrap**
- **其他 Web 前端技术**

建议根据实现难度选择：

- **简单实现方案**：HTML + CSS + JavaScript + Bootstrap。
- **较完整实现方案**：Vue + Axios + 后端 REST API。

### 5.4 并发与性能方案

文档允许自由选择合理方案，包括：

- **数据库悲观锁**
- **数据库乐观锁**
- **程序并发控制**
- **缓存机制**
- **其他合理方案**

对于本项目，推荐至少使用一种数据库级别的并发控制方案，避免只在前端或普通业务代码中判断余量。

---

## 6. 推荐系统模块划分

### 6.1 后端模块

建议划分为：

- **用户认证模块**：游客实名登录、管理员登录。
- **场馆信息模块**：开放信息、参观须知、通知公告。
- **预约配置模块**：预约日期、时段、总名额、时段名额、开放时间。
- **预约核心模块**：提交预约、名额扣减、重复预约校验、上限校验。
- **预约记录模块**：用户预约记录、后台预约名单、导出功能。
- **统计模块**：剩余名额、成功预约数量、分时段统计。
- **系统状态模块**：预约活动状态、当前请求量或简单运行状态。

### 6.2 前端页面

建议至少包含：

- **游客登录页**
- **游客首页/场馆信息页**
- **预约日期与时段选择页**
- **预约结果页**
- **个人中心页**
- **管理员登录页**
- **后台首页**
- **场馆信息管理页**
- **预约活动配置页**
- **预约记录管理页**
- **统计与系统状态页**

---

## 7. 推荐数据库设计方向

### 7.1 用户表

用于保存游客实名登录信息。

建议字段：

- **用户 ID**
- **姓名，可选**
- **身份证号**
- **手机号**
- **创建时间**

### 7.2 管理员表

用于后台登录。

建议字段：

- **管理员 ID**
- **用户名**
- **密码**
- **角色**
- **创建时间**

### 7.3 场馆信息表

用于保存开放信息和参观须知。

建议字段：

- **场馆 ID**
- **场馆名称**
- **开放时间说明**
- **参观须知**
- **地址**
- **状态**
- **更新时间**

### 7.4 公告通知表

用于保存特殊展览、临时闭馆等通知。

建议字段：

- **通知 ID**
- **标题**
- **内容**
- **类型**
- **发布时间**
- **是否启用**

### 7.5 预约活动表

用于保存某一天或某阶段的预约规则。

建议字段：

- **活动 ID**
- **预约日期**
- **每日总名额**
- **预约开始时间**
- **预约结束时间**
- **单人预约上限**
- **单日可预约次数**
- **状态：未开始、进行中、已结束、禁用**

### 7.6 预约时段表

用于保存每个活动下的分时时段和名额。

建议字段：

- **时段 ID**
- **活动 ID**
- **时段名称**
- **入场开始时间**
- **入场结束时间**
- **总名额**
- **已预约人数**
- **剩余名额**
- **版本号，用于乐观锁**

### 7.7 预约记录表

用于保存用户预约结果。

建议字段：

- **预约 ID**
- **预约编号**
- **用户 ID**
- **活动 ID**
- **时段 ID**
- **身份证号**
- **手机号**
- **预约状态**
- **预约时间**
- **二维码内容或凭证码**

建议添加唯一约束：

- **用户 ID + 活动 ID + 时段 ID**，防止同一用户重复预约同一时段。
- 或 **身份证号 + 预约日期**，根据业务规则限制单日重复预约。

---

## 8. 推荐并发处理方案

本项目评分重点是“不超额、不重复、名额准确”，因此预约接口必须重点设计。

### 8.1 推荐方案一：MySQL 乐观锁

在预约时段表中增加 `version` 字段和 `booked_count` 字段。

预约时执行类似逻辑：

1. 查询预约活动和时段是否有效。
2. 校验是否在预约时间范围内。
3. 校验用户是否重复预约。
4. 使用带条件的更新语句扣减名额。
5. 更新成功后再插入预约记录。
6. 如果更新失败，说明名额已满或发生并发冲突。

核心思想：

- **只有当剩余名额大于 0 时才允许更新**。
- **更新操作由数据库保证原子性**。

### 8.2 推荐方案二：MySQL 悲观锁

在事务中使用行级锁锁定对应预约时段。

流程：

1. 开启事务。
2. 查询预约时段并加锁。
3. 判断剩余名额。
4. 判断是否重复预约。
5. 扣减名额。
6. 插入预约记录。
7. 提交事务。

优点是逻辑直观，缺点是高并发下性能可能不如乐观锁。

### 8.3 推荐选择

建议优先选择：

- **数据库条件更新 + 唯一索引防重复 + 事务控制**

这样既能防止超额预约，也能防止重复预约，适合课程大作业实现和演示。

---

## 9. 评分标准拆解

总分 100 分。

### 9.1 前后端功能完整性：30 分

需要覆盖：

- **后台管理功能完整**
- **用户端预约流程完整**
- **时间规则生效**
- **名额规则生效**
- **限购规则生效**

### 9.2 高并发与稳定性：25 分

需要证明：

- **支持高并发访问**
- **接口响应正常**
- **系统无崩溃**
- **系统无卡死**

需要准备性能测试报告和演示过程。

### 9.3 数据一致性：30 分

需要保证：

- **无超额预约：15 分**
- **无重复预约：10 分**
- **名额数据精准一致：5 分**

这是最重要的评分点之一。

### 9.4 虚拟机部署：10 分

需要完成：

- **VMware 固定 8GB 内存配置正确**
- **Linux 环境搭建完成**
- **JDK 和 MySQL 安装完成**
- **项目可正常访问**

### 9.5 文档规范：5 分

需要保证：

- **文档齐全**
- **格式规范**
- **统一 PDF 格式提交**

---

## 10. 必须提交的产出物

最终需要提交一个压缩包，包含以下内容。

### 10.1 系统设计文档

必须包含：

- **需求描述**
- **功能模块**
- **系统架构**
- **数据库设计**
- **核心业务流程**
- **并发处理方案**
- **三种方案对比及选型理由**

### 10.2 功能测试报告

必须包含：

- **测试用例**
- **测试步骤**
- **预期结果**
- **实际结果**
- **测试截图**

### 10.3 性能测试报告

必须包含：

- **并发测试方案**
- **测试工具使用说明**
- **压测结果**
- **响应时间统计**
- **稳定性结论**

可使用 JMeter、Apifox、Postman Collection Runner 或其他压测工具。

### 10.4 系统演示视频

视频需要展示：

- **后台配置**
- **前端预约**
- **并发测试**
- **部署环境**
- **数据核对**

要求：

- **视频清晰**
- **声音正常**
- **时长不超过 5 分钟**

### 10.5 源代码工程文件

必须保证：

- **完整可运行**
- **可以直接编译**
- **可以部署启动**
- **不缺失配置**
- **不缺失依赖**
- **不缺失脚本**

---

## 11. 最终提交要求

### 11.1 压缩包命名

所有成果打包为一个压缩包，命名格式为：

```text
学号-姓名-博物馆预约系统.zip
```

或：

```text
学号-姓名-博物馆预约系统.rar
```

### 11.2 文档格式

所有文档统一使用：

- **PDF 格式**

### 11.3 代码要求

代码必须：

- **可直接运行**
- **配置完整**
- **依赖完整**
- **脚本完整**
- **能够在 Linux 虚拟机中部署运行**

---

## 12. 建议完成顺序

建议按照以下顺序开发：

1. **搭建 Spring Boot + MySQL 基础项目**
2. **设计数据库表结构**
3. **实现管理员登录和后台基础页面**
4. **实现场馆信息和公告管理**
5. **实现预约活动和分时时段配置**
6. **实现游客实名登录**
7. **实现游客查看信息和剩余名额**
8. **实现核心预约接口**
9. **加入并发控制和防重复预约机制**
10. **实现个人中心和预约凭证二维码**
11. **实现后台预约记录、导出和统计功能**
12. **准备功能测试用例和截图**
13. **进行并发压测并记录结果**
14. **部署到 VMware Linux 虚拟机**
15. **录制系统演示视频**
16. **整理 PDF 文档和最终压缩包**

---

## 13. 最低可交付版本范围

如果时间紧张，至少应完成以下内容，保证能覆盖主要评分点。

### 13.1 后台最低功能

- **管理员登录**
- **场馆信息维护**
- **预约日期和时段配置**
- **每日和时段名额配置**
- **预约记录查看**
- **剩余名额查看**

### 13.2 用户端最低功能

- **身份证号 + 手机号登录**
- **查看场馆信息**
- **选择日期和时段预约**
- **预约结果提示**
- **查看我的预约记录**
- **预约凭证展示**

### 13.3 后端最低保障

- **防止超额预约**
- **防止重复预约**
- **预约时间范围校验**
- **单人预约上限校验**
- **事务控制**
- **数据库唯一约束**

### 13.4 部署最低要求

- **Linux 虚拟机固定 8GB 内存**
- **JDK 安装成功**
- **MySQL 安装成功**
- **项目能够启动**
- **宿主机能够访问系统页面和接口**

---

## 14. 关键风险点

### 14.1 超额预约风险

如果只通过前端判断剩余名额，或者后端先查余量再普通更新，容易在并发场景下出现超额预约。

必须使用数据库事务、锁或原子条件更新解决。

### 14.2 重复预约风险

如果只在业务代码中查询是否预约过，而没有数据库唯一约束，并发场景下可能插入重复记录。

建议业务校验和数据库唯一索引同时使用。

### 14.3 时间规则失效风险

预约开始时间、结束时间、活动状态、预约通道状态都需要在后端统一校验，不能只依赖前端按钮控制。

### 14.4 部署环境风险

项目最终必须在 Linux 虚拟机运行，不能只在本机开发环境运行。

需要提前验证：

- **端口是否开放**
- **数据库连接是否正确**
- **JDK 版本是否兼容**
- **MySQL 表结构是否导入成功**
- **宿主机能否访问虚拟机服务**

---

## 15. 结论

本次大作业不是单纯做页面，而是要完成一个具备真实业务逻辑的预约系统。重点不只是“能预约”，还要证明系统在高并发下仍然能够保证数据一致性。

最核心的完成目标是：

- **后台能配置预约规则和查看预约数据**
- **用户能完成实名登录、预约和查看凭证**
- **系统严格控制时间、名额和重复预约**
- **并发场景下不出现超额预约**
- **项目能部署到 8GB VMware Linux 虚拟机中运行**
- **最终提交完整代码、设计文档、测试报告、性能报告和演示视频**
