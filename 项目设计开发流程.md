# 省博物馆参观预约系统设计与开发流程

## 1. 当前项目定位

本项目是一个基于前后端分离架构的 Web 大作业项目。

技术栈为：

```text
前端：Vue + Axios + Bootstrap
后端：Spring Boot
数据库：MySQL
部署：VMware + Linux + JDK + MySQL
```

整体调用关系为：

```text
Vue 前端页面
    ↓ Axios HTTP 请求
Spring Boot REST API 后端
    ↓ JDBC / MyBatis 操作数据库
MySQL 数据库
```

本项目不是单纯做页面，而是要完成一个可以运行、可以演示、可以压测、可以部署的省博物馆分时预约系统。

---

## 2. 作业评分要求对开发流程的影响

根据作业要求，评分重点包括：

- **前后端功能完整性：30 分**
- **高并发与稳定性：25 分**
- **数据一致性：30 分**
- **虚拟机部署：10 分**
- **文档规范：5 分**

因此开发优先级不能只看页面效果，而应按以下顺序安排：

```text
数据一致性 > 后端核心预约逻辑 > 管理后台配置 > 用户端预约流程 > 前端美观 > 部署和文档
```

其中最关键的是：

- **不能超额预约**
- **不能重复预约**
- **名额数据必须精准一致**
- **高并发场景下系统不能崩溃**

---

## 3. 总体开发顺序

推荐开发流程为：

```text
1. 需求拆分
2. 系统设计文档初稿
3. 数据库设计
4. 后端接口设计
5. 后端核心功能实现
6. 管理员后台功能实现
7. 用户端预约功能实现
8. 前端页面开发与接口联调
9. 并发与数据一致性测试
10. 虚拟机部署
11. 文档、测试报告和演示视频整理
```

不建议先完整写前端页面。

原因是本项目的核心评分点在后端业务规则、数据库一致性和并发控制上。如果先做完整前端，后续数据库字段、接口格式或业务规则变动时，前端会反复修改。

---

## 4. 第一阶段：需求拆分

系统分为两个端：

- **游客端**
- **管理员端**

### 4.1 游客端需求

游客端需要实现：

- **身份证号 + 手机号实名登录**
- **查看场馆开放信息**
- **查看参观须知**
- **查看通知公告**
- **查看可预约日期**
- **查看分时时段和剩余名额**
- **选择参观日期和入场时段**
- **提交预约请求**
- **查看预约结果**
- **查看我的预约记录**
- **查看预约成功凭证和二维码**

预约结果至少要覆盖：

- **预约成功**
- **名额已满**
- **已超过预约上限**
- **未到预约时间**
- **预约已结束**
- **重复预约失败**

### 4.2 管理员端需求

管理员端需要实现：

- **管理员登录**
- **场馆信息管理**
- **参观须知维护**
- **公告通知管理**
- **预约活动配置**
- **每日总名额配置**
- **分时时段配置**
- **时段名额配置**
- **单人预约上限配置**
- **预约开始时间和结束时间配置**
- **预约通道启用 / 禁用**
- **预约记录查看**
- **预约名单导出**
- **预约统计数据查看**
- **系统状态查看**

---

## 5. 第二阶段：系统设计文档初稿

系统设计文档不要等项目最后才写，应该在开发前先生成初稿。

系统设计文档必须包含：

- **需求描述**
- **功能模块**
- **系统架构**
- **数据库设计**
- **核心业务流程**
- **并发处理方案**
- **三种方案对比及选型理由**

建议先建立：

```text
docs/系统设计文档.md
```

开发过程中逐步完善，最终转换为 PDF 提交。

---

## 6. 第三阶段：数据库设计

数据库设计应优先完成，因为后端接口和前端页面都依赖数据库结构。

正式系统建议至少包含以下表：

```text
admin_user              管理员表
visitor                 游客表
museum_info             场馆信息表
notice                  通知公告表
reservation_activity    预约活动表
reservation_slot        分时时段名额表
reservation_record      预约记录表
request_log             请求日志或系统状态表，可选
```

### 6.1 管理员表 admin_user

用于管理员后台登录。

建议字段：

- **id**
- **username**
- **password**
- **role**
- **status**
- **created_at**

### 6.2 游客表 visitor

用于保存游客实名信息。

建议字段：

- **id**
- **name**
- **id_card**
- **phone**
- **created_at**

### 6.3 场馆信息表 museum_info

用于保存场馆开放信息、地址、参观须知等。

建议字段：

- **id**
- **name**
- **address**
- **open_info**
- **rules**
- **status**
- **updated_at**

### 6.4 通知公告表 notice

用于保存特殊展览、临时闭馆等通知。

建议字段：

- **id**
- **title**
- **content**
- **type**
- **enabled**
- **created_at**
- **updated_at**

### 6.5 预约活动表 reservation_activity

用于保存某一天或某阶段的预约规则。

建议字段：

- **id**
- **activity_name**
- **visit_date**
- **daily_capacity**
- **person_limit**
- **booking_start**
- **booking_end**
- **status**
- **created_at**
- **updated_at**

### 6.6 分时时段表 reservation_slot

这是数据一致性最关键的表之一。

建议字段：

- **id**
- **activity_id**
- **visit_date**
- **slot_name**
- **start_time**
- **end_time**
- **total_capacity**
- **booked_count**
- **enabled**
- **version**
- **created_at**
- **updated_at**

其中：

- **total_capacity** 表示时段总名额。
- **booked_count** 表示已预约人数。
- **total_capacity - booked_count** 表示剩余名额。
- **version** 可用于乐观锁。

### 6.7 预约记录表 reservation_record

用于保存用户预约成功后的记录。

建议字段：

- **id**
- **reservation_no**
- **visitor_id**
- **activity_id**
- **slot_id**
- **id_card**
- **phone**
- **visit_date**
- **slot_name**
- **status**
- **qr_content**
- **created_at**

必须考虑唯一约束：

```text
id_card + visit_date
```

或：

```text
visitor_id + activity_id
```

用于防止同一游客重复预约。

---

## 7. 第四阶段：后端接口设计

前后端分离项目中，前端不直接操作数据库，而是通过 Axios 调用 Spring Boot 的 REST API。

### 7.1 游客端接口

建议接口：

```text
POST /api/visitor/login
游客实名登录

GET /api/museum/info
查询场馆信息

GET /api/notices
查询公告通知

GET /api/slots
查询可预约时段

POST /api/reservations
提交预约

GET /api/reservations/my
查询我的预约记录
```

### 7.2 管理员端接口

建议接口：

```text
POST /api/admin/login
管理员登录

GET /api/admin/museum/info
查询场馆信息

PUT /api/admin/museum/info
修改场馆信息

GET /api/admin/notices
查询公告

POST /api/admin/notices
新增公告

PUT /api/admin/notices/{id}
修改公告

DELETE /api/admin/notices/{id}
删除公告

GET /api/admin/activities
查询预约活动

POST /api/admin/activities
新增预约活动

PUT /api/admin/activities/{id}
修改预约活动

GET /api/admin/slots
查询时段名额

POST /api/admin/slots
新增预约时段

PUT /api/admin/slots/{id}
修改预约时段

GET /api/admin/reservations
查询预约名单

GET /api/admin/reservations/export
导出预约名单

GET /api/admin/summary
后台统计

GET /api/admin/status
系统状态查看
```

---

## 8. 第五阶段：后端核心功能实现

后端优先实现核心业务逻辑，尤其是预约接口。

推荐后端开发顺序：

```text
1. 基础项目结构
2. 数据库初始化脚本
3. 通用返回结果 ApiResponse
4. 管理员登录
5. 场馆信息管理
6. 公告通知管理
7. 预约活动管理
8. 预约时段管理
9. 游客实名登录
10. 查询可预约时段
11. 核心预约接口
12. 我的预约记录
13. 后台预约记录与统计
14. 导出预约名单
```

### 8.1 核心预约接口流程

提交预约时，后端必须执行以下校验：

```text
1. 校验身份证号、手机号格式
2. 校验预约时段是否存在
3. 校验预约活动是否有效
4. 校验预约通道是否开启
5. 校验当前时间是否到达预约开始时间
6. 校验当前时间是否超过预约结束时间
7. 校验是否重复预约
8. 校验是否超过单人预约上限
9. 原子扣减名额
10. 插入预约记录
11. 返回预约编号和二维码凭证
```

这些校验必须放在后端，不能只依赖前端页面判断。

---

## 9. 第六阶段：并发与数据一致性设计

本项目评分中数据一致性占比很高，所以必须专门设计。

### 9.1 不能使用的错误方式

错误方式：

```text
先查询剩余名额
如果剩余名额 > 0
再普通更新 booked_count
```

原因：高并发下多个请求可能同时读到相同剩余名额，导致超额预约。

### 9.2 推荐方案

推荐使用：

```text
数据库唯一约束 + 事务控制 + 条件更新扣减名额
```

核心 SQL 思路：

```sql
UPDATE reservation_slot
SET booked_count = booked_count + 1,
    version = version + 1
WHERE id = ?
  AND enabled = 1
  AND booked_count < total_capacity;
```

如果更新成功，说明抢到名额。

如果更新影响行数为 0，说明：

- **名额已满**
- **预约通道关闭**
- **时段状态无效**

重复预约则通过数据库唯一约束控制，例如：

```text
id_card + visit_date
```

这样即使并发请求同时进入，也不会插入重复预约记录。

### 9.3 三种并发方案对比

系统设计文档中需要写三种方案对比。

#### 方案一：数据库悲观锁

做法：

```text
事务中查询预约时段并加锁，然后判断名额、扣减名额、插入记录。
```

优点：

- **逻辑直观**
- **数据安全性强**

缺点：

- **高并发下锁等待较多**
- **性能可能下降**

#### 方案二：数据库乐观锁

做法：

```text
使用 version 字段控制更新，更新时判断 version 是否一致。
```

优点：

- **适合读多写少场景**
- **锁冲突相对较少**

缺点：

- **失败后可能需要重试**
- **代码逻辑稍复杂**

#### 方案三：条件更新 + 唯一约束 + 事务

做法：

```text
使用数据库条件更新保证 booked_count 不超过 total_capacity，使用唯一约束防重复预约，整个流程放在事务中执行。
```

优点：

- **实现相对简单**
- **能防止超额预约**
- **能防止重复预约**
- **适合课程大作业实现和演示**

缺点：

- **极高并发场景下仍依赖数据库性能**

最终建议选择：

```text
方案三：条件更新 + 唯一约束 + 事务
```

---

## 10. 第七阶段：管理员后台开发

管理员后台应该先于用户端完善，因为游客预约依赖后台配置数据。

推荐开发顺序：

```text
1. 管理员登录
2. 后台首页统计
3. 场馆信息管理
4. 公告通知管理
5. 预约活动配置
6. 分时时段名额配置
7. 预约通道启用 / 禁用
8. 预约记录查看
9. 预约名单导出
10. 系统状态查看
```

后台页面应能体现作业要求中的：

- **每日开放名额配置**
- **分时预约时段配置**
- **单人预约上限配置**
- **预约起止时间配置**
- **预约通道管理**
- **实时剩余名额查看**
- **成功预约记录查看**
- **统计数据查看**

---

## 11. 第八阶段：用户端开发

用户端开发顺序建议为：

```text
1. 用户实名登录页
2. 场馆首页
3. 公告通知展示
4. 可预约日期和时段展示
5. 提交预约页面
6. 预约结果提示
7. 我的预约记录
8. 预约凭证和二维码展示
```

用户端必须完整覆盖以下流程：

```text
实名登录
    ↓
查看场馆信息和预约规则
    ↓
选择参观日期和入场时段
    ↓
提交预约
    ↓
查看预约结果
    ↓
查看我的预约记录和二维码凭证
```

---

## 12. 第九阶段：前后端联调

前后端联调不应该等所有代码写完后再进行。

推荐方式是：

```text
一个模块完成后，立即联调。
```

例如：

```text
后端完成 /api/slots
    ↓
用浏览器或工具测试接口
    ↓
前端开发预约时段页面
    ↓
Axios 请求 /api/slots
    ↓
页面正常显示时段和剩余名额
```

联调重点：

- **接口地址是否正确**
- **请求方法是否正确**
- **请求参数是否正确**
- **返回字段是否和前端一致**
- **失败提示是否能正常展示**

---

## 13. 第十阶段：功能测试

作业要求提交功能测试报告，必须包含：

- **测试用例**
- **测试步骤**
- **预期结果**
- **实际结果**
- **测试截图**

建议边开发边记录测试点。

### 13.1 示例测试用例

#### 用户预约成功测试

测试步骤：

```text
1. 输入身份证号和手机号登录
2. 查看可预约时段
3. 选择有剩余名额的时段
4. 点击预约
5. 查看我的预约记录
```

预期结果：

```text
预约成功，生成预约编号和二维码凭证。
```

#### 重复预约失败测试

测试步骤：

```text
1. 使用同一身份证号登录
2. 对同一天再次提交预约
```

预期结果：

```text
系统提示重复预约失败。
```

#### 名额已满测试

测试步骤：

```text
1. 将某个时段名额设置为 1
2. 第一个用户预约成功
3. 第二个用户继续预约同一时段
```

预期结果：

```text
第二个用户预约失败，提示名额已满。
```

---

## 14. 第十一阶段：性能测试和并发测试

作业要求提交性能测试报告，必须包含：

- **并发测试方案**
- **工具使用**
- **压测结果**
- **响应时间**
- **稳定性结论**

建议使用：

- **JMeter**
- **Apifox**
- **Postman Runner**
- **简单脚本压测**

### 14.1 高并发预约测试

测试目标：验证不会超额预约。

测试方法：

```text
1. 设置某个时段总名额为 10
2. 同时发送 100 个预约请求
3. 查看成功预约数量
4. 查看 reservation_slot.booked_count
5. 查看 reservation_record 成功记录数
```

预期结果：

```text
成功预约数 <= 10
booked_count = 成功预约记录数
系统不崩溃
接口正常返回
```

### 14.2 重复预约压测

测试目标：验证不会重复预约。

测试方法：

```text
1. 使用同一身份证号
2. 同时发送多次预约请求
```

预期结果：

```text
最多只有一次成功，其余全部失败。
```

### 14.3 响应时间记录

性能报告中应记录：

- **并发用户数**
- **总请求数**
- **成功请求数**
- **失败请求数**
- **平均响应时间**
- **最大响应时间**
- **系统是否崩溃**
- **数据库数据是否一致**

---

## 15. 第十二阶段：虚拟机部署

虚拟机部署占 10 分，不能最后才做。

部署目标：

```text
Windows 宿主机浏览器
    ↓
访问 VMware Linux 虚拟机 IP
    ↓
打开博物馆预约系统
```

### 15.1 虚拟机环境要求

必须满足：

- **VMware 虚拟机**
- **固定 8GB 内存，即 8192MB**
- **Linux 系统，Ubuntu 或 CentOS**
- **安装 JDK**
- **安装 MySQL**
- **项目部署到虚拟机中**
- **宿主机可以访问系统功能**

### 15.2 推荐部署流程

```text
1. 前端执行 npm.cmd run build
2. 后端执行 mvn -DskipTests package
3. 将前端 dist 集成到后端或使用 Nginx 部署
4. 在虚拟机中安装 JDK 和 MySQL
5. 创建数据库并导入表结构
6. 上传后端 jar 包
7. 设置数据库连接配置
8. 执行 java -jar 启动项目
9. Windows 浏览器访问虚拟机 IP
```

对大作业来说，推荐简单部署方式：

```text
Vue 打包后集成到 Spring Boot 静态资源中，最终只启动一个 jar 包。
```

---

## 16. 第十三阶段：提交材料整理

最终提交一个压缩包，命名格式：

```text
学号-姓名-博物馆预约系统.zip
```

或：

```text
学号-姓名-博物馆预约系统.rar
```

必须包含：

```text
1. 系统设计文档 PDF
2. 功能测试报告 PDF
3. 性能测试报告 PDF
4. 系统演示视频
5. 完整源代码工程文件
```

### 16.1 系统设计文档

必须包含：

- **需求描述**
- **功能模块**
- **系统架构**
- **数据库设计**
- **核心业务流程**
- **并发处理方案**
- **三种方案对比及选型理由**

### 16.2 功能测试报告

必须包含：

- **测试用例**
- **测试步骤**
- **预期结果**
- **实际结果**
- **测试截图**

### 16.3 性能测试报告

必须包含：

- **并发测试方案**
- **工具使用说明**
- **压测结果**
- **响应时间**
- **稳定性结论**

### 16.4 系统演示视频

必须展示：

- **后台配置**
- **前端预约**
- **并发测试**
- **部署环境**
- **数据核对**

要求：

- **视频清晰**
- **声音正常**
- **时长不超过 5 分钟**

---

## 17. 最推荐的实际执行计划

从当前 Demo 出发，建议按以下顺序继续推进：

```text
1. 生成系统设计文档初稿
2. 重构并完善数据库表结构
3. 明确完整接口清单
4. 实现管理员登录和后台基础接口
5. 实现场馆信息管理
6. 实现公告通知管理
7. 实现预约活动和时段配置
8. 完善核心预约接口
9. 实现用户端页面
10. 实现管理员后台页面
11. 完成前后端联调
12. 进行并发压测
13. 整理测试报告
14. 部署到 VMware Linux
15. 录制演示视频
16. 打包最终提交材料
```

---

## 18. 常见误区提醒

### 18.1 Spring Boot 不等于前后端分离

正确理解：

```text
Spring Boot 是后端框架。
前后端分离是一种项目架构方式。
```

### 18.2 Vue 不能直接连接 MySQL

正确流程：

```text
Vue → Axios → Spring Boot → MySQL
```

### 18.3 前端判断不能防止超额预约

前端可以显示剩余名额，但真正防止超额预约必须依靠：

```text
后端业务校验 + 数据库原子更新 / 锁 / 事务
```

### 18.4 登录不只是页面跳转

正式系统中，登录后后端应能识别当前用户身份。

Demo 阶段可以简化，但正式系统应至少保存用户身份信息并用于预约校验。

### 18.5 并发稳定不只是页面不卡

本作业中的高并发重点是：

```text
多人同时预约时，系统不崩溃，数据库不出错，名额不超卖，记录不重复。
```

---

## 19. 阶段结论

当前项目已经完成 Demo 验证，证明以下技术链路可用：

```text
Vue 前端 → Axios 请求 → Spring Boot 后端 → MySQL 数据库
```

下一阶段应开始正式系统设计和功能实现。

推荐下一步：

```text
生成系统设计文档初稿，并同步完善数据库设计与接口清单。
```
